name: widgetbook
description: AMDS component gallery — every Amds* widget in light + dark.
version: 1.0.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.6.0
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter
  amds_ui:
    path: ../packages/amds_ui

dev_dependencies:
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
