name: amds_core
description: AMDS shared utilities — Result/Failure, formatters, validators. Pure Dart, no Flutter.
version: 1.0.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.6.0

dependencies:
  intl: ^0.19.0
  meta: ^1.15.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
