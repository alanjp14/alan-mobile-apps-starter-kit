name: amds_motion
description: AMDS v1.0 motion system — route transitions, entrances, micro-interactions. Reduce-motion aware, transform/opacity only.
version: 1.0.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.6.0
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter
  amds_tokens:
    path: ../amds_tokens

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter
